// Barrel export file for all types
// This allows imports like: import { QuranVerse } from '../../types'

// Re-export all types from app.ts
export * from './app';

// Re-export from supabase types if needed
// export * from './supabase';
